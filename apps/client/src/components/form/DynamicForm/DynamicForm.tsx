import React from 'react';

/**
 * What I Need
 *
 * I need a dynamic form that allows for the user to enter an arbitrary number of fields, which themselves
 * are used to represent the fields of a static form.
 */
export const DynamicForm = () => {
  return <h1>Dynamic Form</h1>;
};
