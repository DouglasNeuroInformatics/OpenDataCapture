import { ErrorMessage } from '@/components/ErrorMessage';

const Completed = () => (
  <ErrorMessage
    message="Our records indicate that this assignment has been canceled"
    subtitle="Assignment Canceled"
    title="Access Denied"
  />
);

export default Completed;
