const __APP_VERSION__: string;
const __GITHUB_REPO_URL__: string;
/** The origin the preview frame is served from, or empty to derive one from the dev server (see `resolvePreviewOrigin`). */
const __PREVIEW_ORIGIN__: string;
