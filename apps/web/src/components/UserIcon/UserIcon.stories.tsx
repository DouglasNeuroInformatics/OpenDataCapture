import type { Meta, StoryObj } from '@storybook/react-vite';

import { UserIcon } from './UserIcon';

type Story = StoryObj<typeof UserIcon>;

export default { component: UserIcon } as Meta<typeof UserIcon>;

export const Default: Story = {};
