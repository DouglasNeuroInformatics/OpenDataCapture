{
  "name": "open-data-capture",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "env-cmd cross-env NODE_ENV=production turbo run build",
    "build:api": "env-cmd cross-env NODE_ENV=production turbo run build --filter=@open-data-capture/api",
    "build:web": "env-cmd cross-env NODE_ENV=production turbo run build --filter=@open-data-capture/web",
    "clean": "fd -H -I --prune \".bun.lockb|.turbo|dist|node_modules\" --exec rm -rf",
    "dev": "bun dev:core",
    "dev:all": "env-cmd cross-env NODE_ENV=development turbo run dev",
    "dev:core": "env-cmd cross-env NODE_ENV=development bunx turbo run dev --filter=@open-data-capture/api --filter=@open-data-capture/web",
    "force-reinstall": "bun clean && bun pm cache && bun install",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "prepare": "husky install",
    "test": "env-cmd turbo run test",
    "ws": "./scripts/workspace.sh $@"
  },
  "devDependencies": {
    "@douglasneuroinformatics/eslint-config": "latest",
    "@douglasneuroinformatics/form-types": "latest",
    "@open-data-capture/types": "workspace:*",
    "@turbo/workspaces": "^1.10.14",
    "bun-types": "^1.0.4",
    "cross-env": "^7.0.3",
    "env-cmd": "^10.1.0",
    "eslint": "^8.50.0",
    "husky": "^8.0.3",
    "prettier": "^3.0.3",
    "prettier-plugin-astro": "^0.12.0",
    "prettier-plugin-tailwindcss": "^0.5.5",
    "turbo": "^1.10.14",
    "typescript": "^5.2.2"
  }
}