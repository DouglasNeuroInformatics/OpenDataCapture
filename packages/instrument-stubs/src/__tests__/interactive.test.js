import { describe, expect, it } from 'vitest';

import { interactiveInstrument } from '../interactive.js';

describe('interactiveInstrument', () => {
  it('should be defined', () => {
    expect(interactiveInstrument).toBeDefined();
  });
});
