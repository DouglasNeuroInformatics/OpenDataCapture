// import { expectTypeOf } from 'expect-type';

// import type { InteractiveInstrument } from '../instrument.interactive.js';

// type T = InteractiveInstrument<{ score: number }>;

// expectTypeOf<T['content']>().toMatchTypeOf<InteractiveInstrument['content']>();
// expectTypeOf<T>().toMatchTypeOf<InteractiveInstrument>();
