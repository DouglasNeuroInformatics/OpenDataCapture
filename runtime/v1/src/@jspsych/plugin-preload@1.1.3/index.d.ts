// This package is itself written in TypeScript, but Rollup tries to resolve JSPsych which does
// not exist, because it is versioned. We will write our own declaration to fix this.

const plugin: any;
export default plugin;
