{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "^build:types", "^db:generate"],
      "outputs": ["dist/**"]
    },
    "build:types": {
      "outputs": ["dist/**"]
    },
    "db:generate": {
      "cache": false
    },
    "db:push": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "dependsOn": ["^build:types"],
      "persistent": true
    },
    "format": {},
    "lint": {
      "dependsOn": ["^build:types", "^db:generate"]
    },
    "test": {}
  }
}
