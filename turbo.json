{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "check-types": {},
    "demo": {
      "dependsOn": ["^build"]
    },
    "dev": {},
    "format": {},
    "lint": {},
    "test": {}
  }
}
